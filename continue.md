# Project Continuation Guide: Bare-Metal MIPS on Nationalchip SoC

## 1. Project Overview & Objective
Write, compile, and execute custom bare-metal MIPS assembly code on a Nationalchip set-top box, progressing from basic execution to serial console (UART) text output.

## 2. Hardware & Environment Specs
* **SoC:** Nationalchip MIPS 32-bit Little-Endian (Chip ID: `5874`).
* **Toolchain:** WSL running MIPS cross-compiler (`mipsel-linux-gnu-*`).
* **Bootloader:** U-Boot (loads binaries via USB FAT32).
* **RAM Base:** `0x80000000` (cached / `kseg0`) and `0xa0000000` (uncached / `kseg1`).

---

## 3. Current Progress & Accomplishments
* **Linker Script Fixed:** Overcame massive 4MB binary padding issues caused by default linker scripts by implementing a custom flat-binary linker script (`link.ld`), successfully shrinking output binaries to under 100 clean bytes.
* **Execution Verification:** U-Boot successfully loads and hands over execution via `go 0x80008000` (or `0xa0008000`), running infinite loops cleanly without exceptions.

---

## 4. Current Obstacle / Roadmap
* **Roadblock:** Direct Memory-Mapped I/O (MMIO) character writes via `sb` to common MIPS UART base addresses (`0xb8000000`, `0xb8100000`, `0xbf800000`, etc.) do not yet print characters to the serial console, despite clean code execution.
* **Next Steps:** 
  1. Identify the correct physical MMIO base address or pinmux configuration for the Nationalchip 5874 SoC UART.
  2. Implement proper UART status register checking (line status register / transmit holding register ready check) rather than blind polling.

* **Update 2026-09-23:** Added `uart_hello.S` (16550-style, LSR THRE poll with timeout, returns to U-Boot with rc 0=sent / 1=timeout) and `build.sh <UART_BASE> <REG_SHIFT>`. `link.ld` now links at `0x80008000` (was `0x0`, which breaks `la`/data refs) and discards `.reginfo`/`.MIPS.abiflags` junk. Old `hello.s` blind sweep abandoned: writing every 4KB page from `0xb0000000` can hit random control regs.
* **UART discovery plan:** U-Boot already drives the UART, so ask it: `bdinfo`, `printenv` (bootargs/earlycon), `coninfo`, `fdt addr`/`fdt print` if present. Test candidates interactively with `mw.b <base> 0x41` before building.

* **Board facts (from boot log):** MIPS 24KEc, SoC 0x8080, U-Boot 2012.04, 38400 baud, no fdt, `memstart=0x80100000`.
* **gd found:** `getgd.bin` returned `$k0 = gd = 0x80F7AF98`. Layout matches stock 2012.04 (`baudrate=0x9600` at +0x08). `gd->jt = 0x80F7B060` (at +0x20). `uboot_hello.S` calls `jt[4]=puts`, `jt[5]=printf`.
* **MILESTONE 2026-09-23:** `uboot_hello.bin` printed text on serial console via jt puts/printf, returned rc=0. First working text output.
* **C workflow:** user doesn't know assembly. Write C in `main.c` with `#include "uboot.h"` (printf/puts/getc/tstc/udelay/get_timer/malloc via jt macros), build with `./buildc.sh main.c app` (needs `-static -no-pie`, distro gcc defaults PIE). All used exports verified on board 2026-09-23 (printf, puts, udelay, tstc, getc; argv works). **Gotcha:** U-Boot is PIC, computes `$gp` from `$t9` on entry, so calls MUST jump via `$t9`. Plain C function pointers (jalr v0) hung the board. Fixed with tail-jump stubs in `exports.S` (linked by `buildc.sh`).
* **Firmware analysis (2026-09-23):** `backup.bin` = 8 MB SPI flash dump, `app_ram.bin` = decompressed main app (7,860,376 B, runs at `0x80008000`). NO Linux: main app is one big bare-metal/RTOS IPTV program. Chip family name in code: "symphony". Model string `Model_06_cs8001b_128m_iptv`.
  * Flash map: `0x0` bootinit, `0x10000` U-Boot, `0xA0000` boot.scr (uImage script), `0xB0000` avcpu.bin (LZMA, AV firmware for 2nd core, runs at `0x87e10000`), `0x130000` 1280x720 PNG logo, `0x300000` maincode header, `0x300040` maincode LZMA.
  * boot.scr: `loadimg 1 lzma 0x300000 0x81500000 0x80008000` then `av_launch`/`cpu 1 release 0x87e10000`, then `go 0x80008000`. Has commented dev hook `fatload usb 0 80008000 maincode.bin`.
  * Tentative MMIO map (from xref.py, NOT verified on HW): `0xbf154000` LEDKB front-panel LED/key controller (ct1642 driver), `0xbf156000`/`0xbf15630c`/`0xbf0a002c` pinmux, `0xbf15c000` kadc (ADC keys), `0xbf500000` ethernet MAC, `0xbf5d0000` busy block (74 refs, unknown), `0xbf157000` (43 refs, unknown). Many helper funcs are MIPS16 (odd addresses like `0x801b4199`), so plain mips32 objdump misses them.
  * Tools: `xref.py <string>...` finds code using a string + nearby `0xbfXXXXXX` addresses. `app_strings.txt` = strings dump.
* **Physical board:** PCB `PCB-CS8051M` (2022-10). Front: STANDBY button, IR receiver, 2 LEDs (power green, WiFi). NO 7-seg display (LEDKB/ct1642 code unused). Rear: DC jack, 3x RCA (CVBS + L/R audio), HDMI, USB-A. WiFi module w/ antenna. No Ethernet. SoC under heatsink, 8-pin SPI flash nearby. UART wires soldered on board.
* **Entry fix:** `_start` now in `start.c`, linked by `buildc.sh` for every program (first regwatch build lacked `_start` and crashed). `buildc.sh` fails if first symbol is not `_start`. Programs only need `main ()`.
* **regwatch.c:** read-only register watcher (`go ${a} <start-hex> [words-hex]`), prints changes live. Plan: find button/IR/GPIO blocks by watching candidates `bf15c000` (kadc; U-Boot log `key_idx=0,val=63` hints ADC key), `bf154000`, `bf157000`, `bf15b000`.
* **HW verified 2026-09-24 (regwatch):** `0xbf15c000` = key ADC, 0x20-byte block mirrored every 0x20. `+0x00`=0x10 ctrl, `+0x04`=0x3f result (6-bit, 63 = no key; matches U-Boot `val=63`), `+0x08`=1. Free-running (firmware just polls +0x04 & 0xff). STANDBY button does NOT change it.
* **Corrections:** `0xbf500000` = clock/reset controller (`ledkb_clk` code touches 0xbf500010/14), NOT ethernet. Firmware helpers `0x802586e1` = read32(a0), `0x802586e5` = write32(a0,a1) (MIPS16). `standby_aomcu` = firmware image loaded to `0xa44c8000` for always-on standby MCU (likely owns IR/standby while in standby).
* **MMIO page hit counts (lui 0xbfXX in maincode):** bf5d 92, bf15 92, bf50 74, bf10 30, bf12 24, bf44 12, bf14 9, bf0a 9, bf41 7, bf13 6.
* **regwatch results 2026-09-24:** `bf157000` = `41077000 004a050c` then zeros (ID/version?). `bf15b000` = `0x111` every word (pad config?). `bf154000` = `12020700 002003e8 3b0000f0` (LEDKB, idle). STANDBY changed nothing in any.
* **U-Boot analysis:** `uboot_part.bin` = flash `0x10000..0xA0000`. File offset 0 = `0x800ffff0` (16 B header, code at `0x80100000`). PIC: GOT at file `0x88af0`, gp = file `0x90ae0`. Tool `ubxref.py <string>`.
  * Boot key check (`0x80106e30`): polls ADC `0xbf15c004` & 0xff, 10 samples, debounce. 0-5 = key1, 6-16 = key2, 17-27 = key3, else none. Then calls gpio_fn(71,1), gpio_fn(70,0) -> pins 70/71 probably the 2 front LEDs.
  * GPIO helpers: fnA `0x80104bcc` sets/clears bit in `bf0a0000` (pin 0-31), `bf0a0010` (32-63), `bf155000` (64-71, bit = pin-64). fnB `0x80104ae0` same but `+4` regs (`bf0a0004`, `bf0a0014`, `bf155004`). One = direction, other = output data (unverified which).
* **VERIFIED ON HW 2026-09-24:** GPIO bank layout: `+0x00` out data, `+0x04` direction (1 = input), `+0x08` input level, `+0x0c` unknown (0xff/0xffffffff). Bank0 `0xbf0a0000` (pins 0-31, all input: dir=ffffffff, in=0x4800). Bank2 `0xbf155000` (pins 64-71): out=0x80, dir=0x3f, in=0x80 -> pins 70/71 outputs, 71 high at U-Boot prompt.
  * **STANDBY button = GPIO pin 11** = bit 11 of `0xbf0a0008`, active low (0x4800 released, 0x4000 pressed).
  * **Power LED is bi-colour, active high: pin 70 = RED, pin 71 = GREEN** (bits 6/7 of `0xbf155000`). Verified with `led.bin`. WiFi LED NOT on these pins (likely driven by WiFi module).
  * STANDBY read from C verified (clean edge detect, 20 ms poll).
  * `board.h` = verified helpers: `led_red ()`, `led_green ()`, `standby_pressed ()`, `wait_standby ()`. `main.c` now = red/green toggle example.
  * `main.c` red/green toggle verified: 35 presses, clean.
  * **BSS fix 2026-09-24:** `start.c` now zeroes `.bss` (`__bss_start`/`__bss_end` in link.ld). Before, uninitialised globals held leftover RAM (first irscan run printed garbage counts). All .bin rebuilt.
  * `irscan.c`: 5 s tight-loop toggle counter on GPIO input regs (bf0a0008, bf0a0018, bf155008) to find IR pin. Candidate: bank0 bit 14 (idle high). Pending run.
  * **irscan result (after bss fix):** 10M samples, 2024/ms, ZERO toggles with remote pressed. IR is NOT a plain GPIO input on banks 0/1/2 -> dedicated IR decoder block or AO MCU. Firmware `ir_value=0x%x` code (`0x8017e3e0`) is UI-level only. `[IRR] stb_power_off` path clears bit 2 of `0xbfedc02c` (always-on/power block?). regwatch `bfedc000 40`: all zeros, no change with remote; safe to read but slow to respond (looked frozen, then exited OK). Conclusion: IR decoder likely unclocked/uninitialised under U-Boot; needs firmware init sequence.
  * **Build infra 2026-09-24:** `libc.c` (memcpy/memmove/memset/memcmp/strlen/strcmp; GCC emits memcpy for struct init) linked by `buildc.sh`, with `-ffunction-sections -fdata-sections -Wl,--gc-sections` so unused funcs drop. link.ld `KEEP(*(.text.start))`.
  * `cpuinfo.c`: CP0 PRId/Config0-3 decode, clock via CP0 Count vs get_timer (1 s), benchmarks (branch loop, mul, div, mem read/write at scratch RAM `0x82000000` cached / `0xa2000000` uncached). **Results:** PRId 0x00019655 = MIPS 24KE (24KEc: DSP ASE rev1, no FPU), MIPS32r2 LE, 64 TLB, L1 I/D 16 KB 4-way 32 B lines, no L2, MIPS16e yes, vectored int yes. Count 324M ticks/s x CCRes 2 = ~648 MHz (clock relies on U-Boot get_timer, itself probably Count-based: not independent). Branch loop 1.5 CPI, 430 MIPS. mul 287M/s, div ~38 cycles. Mem: L1 read 736 / write 1030 MB/s; DDR cached 165/175; uncached read 48 MB/s (~83 ns latency), write 175.
  * **HDMI OUTPUT WORKS 2026-09-24** (grey/black screen, capture card detects signal). Recipe from U-Boot prompt: the 9 `setenv avenv_*` lines from boot.scr, then `loadimg 1 lzma 0xb0000 0x81500000 0x87e10000`, `av_launch 0x87e10000` (inits HDMI tx anx8560-style, reads EDID, display open), `cpu 1 release 0x87e10000`. Output: 1920x1080 interlaced YCbCr444 24-bit. Plain `vid start`/`vid test` without AV core: no output / hang.
  * Display registers (U-Boot display-driver code 0x8014xxxx): `0xbf44xxxx` 77 regs (main display ctrl?), `0xbf47xxxx` 32 regs, `0xbf26xxxx` 49 regs with groups at +0x100/+0x1100/+0x1500 (per layer?), `0xbf27xxxx` 15, `0xbf41`, `0xbf46`. `0xbf157000` = HDMI analog (driver writes 0x41071080/0x41071000 "hdmi analog reset"). ubxref.py now also handles global-GOT refs + 16-instr window.
  * `avstart.txt` -> `avstart.scr` (via `mkscript.py`, legacy uImage script like boot.scr): `fatload usb 0 0x80100000 avstart.scr; source 0x80100000` runs whole AV/HDMI init. `regdump.c`: one-shot dump of display candidate blocks (skips zero lines). Next: dump regs with display live, find layer enable / framebuffer addr / pixel format.
  * **Display regdump (display live, 1080i):** `bf44004c`=0x07800438 (1920x1080), `bf4400b8`=0x021c0780 (540 field lines x 1920), `bf4400bc`=0x012002d0 (720x288 SD path), `bf440040/44`=0x10 (bg colour Y? untested), `bf440110`=0x07800780. `bf470000` = HDMI timing (1920/1080/0x898). `bf460000` = coefficient tables (CVBS/CSC).
  * **Layer buffer-address regs (from U-Boot `set_start_addr` 0x80150704, jump table per layer 0-9):** value = phys_addr >> 3 in bits 0..25. layer1 -> `bf44102c`; layer2 -> `bf441020` (even field) + `bf44101c` (odd field = even + a3); layer5 -> `bf441030`; layer6 -> `bf441028`; layer7 -> `bf441024`; layers 0/3/4/9 = "No need to set start addr". `bf441008` bit 28 set by a 1-bit setter (layer on/off?). Current: 101c=0x2c000, 1020=0x20000, 1028=0x50000, 1030=0x50000 (x8 = phys 0x160000/0x100000/0x280000).
  * Blind tests failed: `mw.l bf440040/44 = 0xeb` no screen change; filling phys 0x100000-0x300000 (0xff/0x80) no change. `accessors.py` maps ~130 U-Boot bf44 field setters (`accessors_bf44.txt`).
  * `originalboot.log` (normal firmware boot): OSD region 0x1c2048 = 1280x720x2 (16 bpp) + 72; two OSD regions; output RESOLUTION_1080p_50HZ; startup pic = 1280x720 PNG from flash + test.mp4. (Log contains WiFi passwords: keep private.) AV core fw (`avcpu.bin`, decompressed) = video decoder only.
  * **Hook plan:** `hookboot.scr` = normal boot + `hook_dump.bin` @0x80004000 (hook_entry.S + hook_dump.c, uses firmware printf from $s2) + `hookpatch.bin` @0x82000000 (patches 0x8009a264 printf("app_entry_task start") -> jal 0x80004000; flushes caches) then `go 0x80008000`. Firmware low-RAM use: only 0x80000000-0x80000dff and 0x80007000. `buildc.sh` now takes 3rd arg load addr; link.ld `LOAD_ADDR` defsym.
  * **HOOK WORKED 2026-09-24** (`hookboot.log`, from MobaXterm RTF). Firmware-live display state vs U-Boot-only: `bf440110`=05000780 (OSD src width 1280 -> 1920), `bf440114`=0xaaa (H scale 1280/1920), `bf440128`=02d00438 (720 -> 1080), `bf44012c`=0xaaa; `bf441028` (layer 6) = 0x1c6ecd -> phys 0x00E37668, `bf441030` (layer 5) = 0x1ff309 -> phys 0x00FF9848 (two OSD planes 1280x720x16bpp, 0x1c21e0 apart). `bf440000` 00000100 -> 10001100, `bf440060` 0 -> 1 (enable bits?), `bf44006c` 0x10 -> 00d70111, `bf440070`/`90` = 010000ff, `bf4400a8` = 30000000, `bf441034` = 9012d0d0, `bf440160` = 1e028000. `bf441040..` = video decoder frame buffers. Output 1080p progressive (`bf440018`=04390001, `bf4400b8`=04380780).
  * `hook_draw.c` (build: `./buildhook.sh hook_draw.c hook_draw`, boot: `hookdraw.scr`): paints 8 test bars (f800 07e0 001f ffff 7c00 83e0 f0f0 0000) into layer6 (vertical) and layer5 (horizontal), holds 15 s. **Result:** screen stayed dark, firmware menu then missing. Both layer addrs started `0001 3030 0001 0001` -> layer regs point to a REGION HEADER (0x48 bytes, matches rgn_size 0x1c2048), not pixels; bars overwrote headers. Next: `hook_hdr.c` / `hookhdr.scr` read-only header dump + follows pointer-like words.
  * **OSD REGION HEADER (verified, menu still OK after read-only hook):** layer6 hdr @0x00e37668: `30300001 00010001 02d00500 050000ff a2a99450 0 a0ff96a8 0`. w0 = format/flags?, w1 = show flags?, w2 = h<<16|w (720x1280), w3 = pitch_px<<16|alpha (1280, 0xff), **w4 = pixel buffer as uncached CPU addr (0xa2a99450)**, w6 = hdr+0x1c2040 (region end / palette?). layer5 hdr @0x00ff9848 same, pixels at 0xa2c5b850. Layers 1/7 regs = 0 (unused).
  * `hook_draw.c` rewritten: draws bars into w4 pixel buffers using w/h/pitch from header (headers untouched).
  * **FIRST PIXELS ON TV 2026-09-24.** Vertical bars visible (layer 6 = visible OSD, on top); layer 5 horizontal bars NOT visible (off/hidden). Colours: f800 red, 07e0/001f/7c00/0000 transparent (black bg), ffff white, 83e0 green, f0f0 pink => **pixel format ARGB1555, bit15 = opaque**. Bars only briefly visible (user screen-recorded), then firmware menu.
  * Graphics lib: `osd.h` (fb_init from layer-6 header, fb_pixel/rect/clear/char/text, RGB() macro -> ARGB1555, TRANSPARENT=0) + `font8x16.h` (from `mkfont.py`, DejaVu Sans Mono Bold, free license). `hook_demo.c` / `hookdemo.scr`: text + gradient + transparent hole + bouncing box until STANDBY, then clears layer and returns. `buildhook.sh <src> <out>` builds hook code (max 12 KB at 0x80004000). Pending run.
  * **hook_demo WORKS** (text, gradients, transparent hole, bouncing box; STANDBY exits).
  * **Ad removal 2026-09-24:** boot ad = MP4 in flash at `0x700000` (header: magic a512, ver "0.0.0.18", "video", date 20250131, URL ota-<vendor>.b-cdn.net/Ads_iptv/...mp4, size at 0x7000fc = 0xab520 (701728), MP4 data from 0x700104 to 0x7ab624). Firmware fetches ad lists via ftp from hisight.top / ota.<vendor>.tv and updates when url version != stb version. User ran `sf erase 0x700000 0x10000` (only that 64 KB sector): ad gone, firmware now shows fallback <vendor> splash. Other data at 0x7b0000+ untouched. Undo: `fatload usb 0 0x82000000 backup.bin; sf probe 0; sf erase 0x700000 0x10000; sf write 0x82700000 0x700000 0x10000`. To stop re-download if WiFi used: block hisight.top, ota.<vendor>.tv, ota-<vendor>.b-cdn.net.
  * **Goal: boot straight into own app.** Route A (preferred): U-Boot av_launch (HDMI) + own OSD init, then change boot.scr at flash 0xa0000 (optionally dual-boot: USB app if present, else stock). Route B: auto-boot with hook that never returns. `osdinit.c` = Route A experiment: header @phys 0x03000000, pixels @0x03001000, writes firmware OSD reg values (0x100-0x12c scaler, 0x6c/70/90/a8/160, 0x1034, 0x1028, 0x60, 0x000) and draws test screen. Run after `source avstart.scr`. (osd.h guard renamed OSD_LIB_H.)
  * **ROUTE A WORKS 2026-09-24:** osdinit showed test screen from plain U-Boot (no stock fw). Issues: picture 2x tall (U-Boot outputs 1080i, 540 lines/field; firmware scale assumed 1080) and pure blue RGB(0,0,255)=0x801f shows transparent (colour key; same seen in hook demo gradient end). Fix: `osdsetup.h` `osd_setup (&fb)` computes scale from `0xbf4400b8` (h<<16|w of output: 540 for 1080i), scale reg = src*4096/dst; osd.h BLUE = RGB(0,0,240). `osdinit.c` now uses it; `tvapp.c` = standalone demo (text, gradients, bouncing box, LED blink, STANDBY/serial exit).
  * Vertical scale in U-Boot 1080i: 0x12c=0xaaa (0x128 dst 1080) -> ~2.7x tall; 0x1555 (dst 540) -> ~5.3x. => zoom ~= value/1024 in this mode (horizontal 0xaaa = correct 1.5x). osdsetup.h now: interlaced (dst_h<=576) -> 0x128 = 720<<16 | 2*dst_h, 0x12c = 1024*2*dst_h/720 (=0x600); overrides `osd_vscale`/`osd_vdst`; `go ${a} <vscale> [<vdst>]` in osdinit. osdinit test screen has 100x100 square for aspect check. `parse_hex ()` moved to libc.c (declared in uboot.h).
  * **Correction after 0x580/0x600/0x620 test:** value x height = const -> vertical reg IS a step (bigger = shorter). Correct 1080i setting: 0x128 = 720<<16 | 1080 (frame height), 0x12c = 720*4096/540 = 0x1555. The earlier 0x1555 failure was because 0x128 dst was 540. osdsetup.h fixed; manual check `go ${a} 1555 438`. Pending confirmation. -> WRONG again (no change with 0x1555).
  * **OSD SCALING SOLVED 2026-09-24:** scaler has 2 modes via `bf440100`: U-Boot 1080i mode `000e0001` (ratios 16.16, size regs src<<16|dst-per-field) vs firmware 1080p `000c0003` (4.12). Working 1080i set (verified: square square, full frame): 100=000e0001, 108=07000000, 10c=0fd20000, 120=00000021, 110=05000780, 114=0000aaaa, 128=02d0021c, 12c=00015555. osdsetup.h now uses this (computed from bf4400b8). Tuning args removed from osdinit.
  * **Dual boot:** `autoboot.txt` -> `autoboot.scr`: AV/HDMI init, `usb start`, `if fatload usb 0 0x80008000 tvapp.bin; then go; fi`, else (or when app exits) loadimg stock maincode + go. **autoboot.scr from USB VERIFIED: tvapp starts, scaled correctly** (stock fallback after STANDBY VERIFIED: stock menu appears). tvapp gradient capped at 247 (blue 248+ = 0x801f colour key). Test via `fatload usb 0 0x82100000 autoboot.scr; source 0x82100000` first; later write to flash boot.scr sector 0xa0000 (backup in backup.bin at 0xa0000).
  * **AUTOBOOT IN FLASH 2026-09-24:** `autoboot.scr` written to flash boot.scr sector 0xa0000 (`sf erase 0xa0000 0x10000; sf write 0x82100000 0xa0000 0x1000`). Power-on with USB stick holding `tvapp.bin` -> own app starts directly (verified; blue gradient fixed). No-stick path = stock firmware (verified from USB-sourced script; confirm from flash). Restore original: `fatload usb 0 0x82000000 backup.bin; sf probe 0; sf erase 0xa0000 0x10000; sf write 0x820a0000 0xa0000 0x10000`.
  * **Workflow for own TV apps:** write C with `#include "board.h"` + `#include "osdsetup.h"`, call `osd_setup (&fb)`, draw with fb_* from osd.h; `./buildc.sh myapp.c tvapp` -> copy `tvapp.bin` to USB -> power on.
  * **1080p goal (user wants 1080p output, then 1920x1080 app):** U-Boot has no mode env; AV init hardcodes 1080i (clock init 0x80149724 picks clock by chip id regs bf5d005c/68; "HD clk 74.25 MHz"). Plan: snapshot regs in both states (`snapranges.h` list: bf500000 clocks, bf510000, bf5d0000, bf157000, bf440000-3000, bf460000, bf470000, bf260000/261000/270000, bf410000), diff, replay firmware 1080p values stepwise. Tools: `snap.c` (U-Boot, after avstart) + `hook_snap.c`/`hooksnap.scr` (firmware), same "S addr: 4 words" lines.
  * Snapshots: `run1.txt` (fw 1080p), `run2.txt` (U-Boot 1080i); `diffsnap.py run1 run2 > snapdiff.txt`. Noise: bf5d0010.. mirrored counter (0ab52a97 vs a346a350), bf260000/bf410000 (video decoder/audio activity). Candidates: bf470000 timing, bf440018/b8/140/144, bf460000 encoder, bf5d0018/1c/38/3c/6c/9c/c4/c8/cc/d4 (clock/PLL?), bf4700d8-f4, bf157004. bf50001c only bits 4-5 differ (ethernet-ish clock gate?).
  * U-Boot HDMI TX: internal regs via GOT -26316 read(reg,&byte) / -25420 write(reg,val); TX reg 0x28/29 = input H active, 0x32/33 = V active, 0x10 bit2 = interlace; for 1920x1080p writes regs 150/151/152 = 13/77/77 (0x80146138). TX auto-detects input format ("video format change"). "HD clk" func 0x80149724 only prints a name (falls through to 74.25 on this chip).
  * `regapply.c`: `go ${a} <groups>` applies fw values: 1 timing, 2 mixer size, 3 encoder, 4 clock/PLL, 5 HDMI misc, 6 = pulse bf157000 bit7 (HDMI analog reset like U-Boot).
  * Test `go ${a} 1 2 4`: no freeze, output STILL 1080i50 (capture). `run3.txt` readback: timing regs bf470000-7c and bf5d0018/1c/6c/c8/cc now equal fw (bf5d003c/c4 status-like, reverted); osdinit then showed ~1.33x tall image (mixer 1080 progressive vs 1080i output). Remaining untried: groups 3, 5, 6.
  * Bisect (fresh boot each, monitor reading): 3 alone -> 1080i50; 5 alone -> 1080i78 @43.8 kHz; 6 alone (bf157000 bit7 pulse) -> 1080i78 @43.6 kHz. 43.8k*2640 = ~115.6 MHz: frame structure unchanged, only HDMI PHY clock changed => monitor rate follows HDMI PHY (bf157000/bf157004), not display clock regs. U-Boot's own reset pulse uses 0x41071080/0x41071000 (bit12 set), ours had bit12 clear. fw PHY values: bf157000=00177000, bf157004=044af50c. Added group 7 = set both via bit7 pulse. Pending: fresh `go ${a} 7`, then `go ${a} 1 2 4 3 5 7`.
  * Bisect 2 (fresh each): groups 1,2,3,4,6 alone -> 1080i50; 5 alone and 7 alone -> 1080i78 43.8 kHz => `bf157004 = 044af50c` alone = video PLL change (~115.6 MHz), still interlaced.
  * **U-Boot HDMI driver decoded:** mode table at link 0x80188858 (36-byte records: Htot W Vtot H HFP HSW HBP vpack flags; idx 0 640x480p60,1 720p60,2 1080i60,3 480p60,4 480i60,5 1080p60,6 1080p50,7 720p50,8 1080i50,9 576p50,10 576i50), lookup func 0x801447e4 takes CEA VIC (2..31). `to_vic(rate, res, aspect)` 0x80144720 (GOT -26216): rate 1=60Hz; res 1 480i,2 576i,3 480p,4 576p,5 720p,6 1080i,7 1080p -> 1080p50 = VIC 31, 1080p60 = 16. `set_mode(dev, cfg)` 0x8014876c = HDMI dev ops[1] (attach 0x80148094 fills ops); dev[0] = priv = *(page+21072) with page = GOT[-32736] (link addr 0x80188af0, value 0x80190000); saved cfg fields 0x10..0x1c at page+21076..; cfg+0x14 rate, +0x18 res, +0x1c aspect. set_mode contains the bf157000 "hdmi analog reset" pulse. Runtime addr = link + gd->reloc_off (gd+0x14).
  * `vicset.c`: `go ${a}` prints state; `go ${a} p50|p60|i50` calls U-Boot set_mode with res 7/6 (checks entry word 0x3c1c0005 first).
  * vicset p50 result: set_mode works (reloc_off 0x0127c000, set_mode runtime 0x813c476c, priv 0x80f7cef0, saved cfg colour 3 rate 0 res 6 aspect 2), TX reconfigures but "Video not stable" -> "configure video error", returns -1. Stable check (0x80142f44): TX reg 0x10 bit5 (0x20) set 3 reads in a row (working 1080i init reached 0x24). With regapply 1 2 3 5 first: same, reg 0x10 flips 0x8/0x0, never bit5 => input pixel clock wrong.
  * Clock measurement via monitor line rate (x2640 = pixel clock; 1080p50 target 56.25 kHz): groups 4+7 -> 44.0k, 4+5 -> 44.0k, 7 -> 43.8k. Group 4 irrelevant. Added group 8 = bf5d005c=b825 (low byte read by U-Boot "HD clk" code = video clock select; U-Boot has 0x14), bf5d0058=1c0186ea, bf50001c=0011ff33, bf500030=0. **Group 8 alone -> 1080i100 56.3 kHz = 148.5 MHz: bf5d005c low byte 0x25 selects the 1080p50 pixel clock** (0x14 = 74.25).
  * `1 2 3 8` then vicset p50: TX reg 0x10 flips 0x8/0x0 (bit2 interlace now clear) but no bit5 -> configure video error, no signal. Monitor mode before vicset still i100 because TX DE generator uses its own VIC timing. Added group 9 = group 5 minus bf157004 (bf4700d8-f4 display->TX link). `1 2 3 9 8` + vicset p50: same failure (0x10 flips 0x8/0x0, no bit5).
  * **HDMI TX regs are MMIO bytes:** bank0 = 0xbf480000+reg, bank1 = 0xbf480100+reg (U-Boot helpers 0x8014132c rd / 0x80141344 wr / 0x80141360 rd1 / 0x8014137c wr1). Tools: `snaptx.c` (U-Boot) + `hook_snaptx.c`/`hooksnaptx.scr` (firmware 1080p), "T addr: 16 bytes" lines, 0x200 bytes. `tx1.txt` (fw) vs `tx2.txt` (ours after p50) diff: **fw TX timing = Htotal 0x0898 = 2200, HFP 0x58 = 88, Vtotal 0x0465 = 1125 => stock firmware actually outputs 1080p60** (despite "RESOLUTION_1080p_50HZ" log). vicset p60 after `1 2 3 9 8`: still unstable. Other TX diffs: 0x07 03->07, 0x43 00->c0, 0x47 60->40, 0x4b 00->04, 0x80-84 (AVI?), bank1 03/04/07/13/14/17/c0/cc (PHY?), 0xa0-0xd7 HDCP. regapply group 10 = clone those 29 TX bytes (no 0x10, no HDCP); group numbers now parsed decimal. Result `1 2 3 9 8 10`: no signal; p60 after: unstable. NOTE: TX 0x26-0x36 already held fw values before clone => that block = MEASURED input timing: our display pipeline delivers correct 1080p60 (2200x1125 @148.5). `1 2 3 9 8 7 10` + p60: still unstable (0x10 flips 0/8, never 0x20). U-Boot `MISC_Reset` (0x801452f8) = TX reg 0x08 <- 2 then 0; added as group 11. `1 2 3 9 8 7 10 11` + p60 and `1 2 3 4 9 8 7 10 11` + p60: still unstable.
  * **run4.txt full diff (ours after attempt vs fw):** bf440140/bf440144 REVERTED to U-Boot 1080i values (021d021d) after group 2 wrote them; TX measured regs 0x2a/0x30/0x31/0x34 (HFP, Vtotal) = 0 after set_mode (before set_mode they measured correct 1080p60). set_mode also rewrote TX 0x07, 0x42/43/48, bank1 03/04/13/14. Suspect U-Boot display/HDMI ISR reprogramming display to 1080i on "video format change". Added group 12 = CP0 Status.IE off. Next: `12 1 2 3 4 9 8 7 10 11` then vicset p60, then re-run groups 2 10 after.
  * vicset loaded after regapply crashed (RI exception, ErrorPC 0x813db918 = U-Boot) -> probably stale I-cache from fatload over same address; set_mode moved into regapply as group 13. `12 1 2 3 4 9 8 7 10 11 13 2 10 11`: still unstable, AND bf440144 read back 021d021d again even with IE off => not ISR; bf440144 = mixer's own line-count readback, mixer still interlaced. Group 14 = remaining bf44 diffs (dc, e0, 124, 164, 1c4, 441000, 441008, 44300c, 44305c). U-Boot max 16 args per command -> split runs. Result `12 1 2 14 3 4 9 8` + `7 10 11 13 2 14 10 11`: still unstable; bf440144 reads 021d021d again (mixer still interlaced), bf440140 reads 800000d5 (live counter?). Group 14 not the interlace switch.
  * **DECISION 2026-09-24: user accepts 1080i output; 1080p work stopped** (too many manual test rounds without remote control of power/console/HDMI info). Keep U-Boot's native 1080i50.
  * **libgfx port 2026-09-24:** user's library `E:\MFoES02w\libs\libgfx` (git repo; Adafruit-GFX-style `LinuxGFX` ARGB8888 API + `DrawReplay` "DRP1" command-stream wire format for remote displays). Added backend `GFX_NC5874` in place (GFX.h/GFX.cpp/DrawReplay.* ifdefs, new `nc5874_std.h/.cpp` freestanding libc/libstdc++/libm subset, CMake option `GFX_NC5874_SUPPORT`). Draws to ARGB8888 RAM surface; swapBuffers converts to OSD ARGB1555 at phys 0x03001000 (hdr 0x03000000), 0x801f nudged to 0x801e. Platform hooks in project `gfx_glue.c` (first-fit heap 0x81600000-0x82ff0000, puts, get_timer). **No FPU: toolchain libgcc float helpers use FPU ops (div.s) -> `softfp/libsoftfp.a` built from compiler-rt 18.1.8 builtins (-msoft-float, 46 routines, verified 0 FPU insns; `stubinc/gnu/stubs-o32_soft.h` empty stub).** `buildgfx.sh app.cpp out` builds C++ apps (checks entry + zero FPU instructions). start.c now runs .init_array (C++ global ctors); uboot.h has extern "C" guards. `gfxdemo.cpp` (shapes, AA, alpha, text, DrawReplay round trip, bouncing ball + fps) built 48860 bytes. Native fbdev/DRM builds of libgfx still compile.
  * **gfxdemo WORKS on box** (all shapes, AA, alpha, text, DRP1 blob 237 B rendered via DRRender). Only 10 fps: full-frame ARGB8888->1555 convert every swap (~40 ms RAM-bound) + per-pixel virtual setPixel for spans. Fixes: common-code span fast paths in writeFastHLine/VLine (direct m_pBuffer writes when m_pBuffer set; stride = m_pitch/4 or width for ARGB8888 canvas; canvas other formats / DrawReplay keep per-pixel path) + NC5874 dirty-rect present (tracked in setPixel & spans, full on multibuffer/clearBuffer, 32-bit pair stores, address-aligned). Demo now erases only old ball + fps label. Known libgfx issue: fillCircle overlapping vertical spans double-blend with alpha<255 -> vertical stripes (all backends). **Result: 93 fps** (was 10). Motion looks like teleporting: loop not synced to 50 fields/s + tearing. Next: vsync. `vsyncprobe.c` result: **bf47008c = pixel-clock scan position within frame, 0..2969999 (2640x1125), wraps per frame (1080i: 25/s; field 2 starts at 1485000)**; bf440140 low bits step ~per field (d3..d7); others static. libgfx NC5874 now waits for next field (nc5874WaitVSync, 50 ms timeout) before converting a non-empty dirty rect. Demo rebuilt (50140 bytes). Pending smoothness test.
  * **1080p STATUS (paused here, for reference):** clock 148.5 MHz OK (group 8), TX measures correct 1080p60 input before set_mode, TX/PHY cloned; blocker = display mixer / timing generator still runs interlaced (bf440144 541 lines/field). Next idea: find U-Boot display driver's own mode-set function (like HDMI set_mode) that programs mixer+timing from (res, rate); HDMI saved cfg = colour 3, rate 2 (=50 Hz), res 6, aspect 2.
  * Remaining targets: IR receiver (not found yet; not on bank0 bits that changed), video/HDMI, audio, WiFi (USB/SDIO?), 2nd CPU core.
  * **IR RECEIVER (2026-09-24, VERIFIED):** block `0xbf151000`, IRQ 40, firmware driver is MIPS16 (found via literal-pool scan for 0xbfXXXXXX words; 32-bit lui scans miss MIPS16). Driver: open 0x80267a08 (cfg byte 0 = mode: 0 = HW NEC decode), mode-0 setup 0x80266a34, timing tables 0x80266130, ISR 0x80266d0c. Regs: +0x00 data (0-15 user, 16-23 key, 24-31 ~key), +0x08 cfg 0x05740215 (prescale 2), +0x0c bit0 repeat, +0x10 int enable, +0x14 status bit0 ready (self-clears after data read), +0x18 top 2 bits cleared, +0x40..0x5c pulse windows (table by 0xbf140020 bit30). U-Boot leaves cfg 0x13270114 (not decoding); fw values fix it. Stock remote user code 0xfe01. Holding a key sends frame, 1 repeat code, then full frames. `ir.h` (ir_init/ir_poll, hold by timing IR_HOLD_MS 250) verified with `irkeys.c`; `irtest.c` = register-level test. App layer: AM_Input_Init 0x8017e1a4, key callback 0x8017e598. Key names not mapped.
  * Tools: `m16dis.sh <start> <end>` (WSL) disassembles MIPS16 ranges of app_ram.bin; `xref2.py <file> <base> <string>` = xref.py for any image (e.g. avcpu.bin at 87e10000).
  * **Hooks inside stock firmware:** hookpatch modes `uio` (site 0x8017e378, after "uio init end") and `irkey` (IR key callback 0x8017e598, `j 0x80004000`; `hook_entry_irkey.S` trampoline re-runs `addiu sp,-24; li v0,0xfe01`, jumps to 0x8017e5a0; runs on every remote key press, IR interrupt context). `buildhook.sh <src> <out> [entry.S]`. **Firmware printf = 0x8018c184** (32-bit; 0x8019c184 was a miscalculation -> RI exception). App silences debug output after boot: byte 0x80772d64 (printf enable) + word 0x808b5280 (1 = UART muted); hooks set both, print, restore. `hook_irsnap.c`/`hookirsnap.txt`, `hook_audsnap.c`/`hookaudsnap.txt` (audranges.h regs, pointer T lines every 10 ms, HDMI TX "X" lines, real PCM buffers).
  * **AUDIO (2026-09-24, VERIFIED HDMI + RCA):** AV core (`avcpu.bin`) does audio decode + output too. Audio out block `0xbf490000`; address/size/pointer regs = phys >> 3 (addresses & 0x3ffffff, sizes/levels & 0xfffff). Buffer 0 = playback ring (fw: phys 0x07aa0000, 256 KB): frame 16 B, word0 = two 16-bit samples (hi, lo), words 1-3 zero; +0x100 read ptr, +0x104 level, +0x10c write ptr readback (read-only), +0x20 reserve, **+0x24 commit = bytes>>3 in bits 0-24 then set bit 31** (AV core 0x87e18180). **Output rate 48 kHz** (so get_timer/Count timings are ~9 % off). +0x80 reset pulse 3,0,0x10,0 (AV core 0x87e16e70). +0x00 ctrl 0x115 idle / 0x305 playing. Buffer 1 = packed 16-bit "PP buf" fed by AV core DMA engine 0xbe900000 (from SPRAM 0xbe98xxxx) - not needed. **HDMI fix: HDMI TX reg 0x51 (MMIO 0xbf480051, I2S SD0-3 enable) must be 0x84 (SD0 only)**; U-Boot leaves 0xbc (fw also 0xbc at boot, switches at playback) -> 12 kHz junk. `audio.h` (audio_start/space/write/stop, AUD_BUF_PHYS overridable, default 0x03800000), `audplay.c` (tone, needs sine256.h from `python mksine.py > sine256.h`), `wavplay.c` (WAV from RAM 0x81600000, PCM16 mono/stereo 8-96 kHz resampled, vol=NN default 100, <reg>=<val> overrides, audio buffers at 0x04000000). Measured: 440.03 Hz clean on HDMI and RCA; WAV plays (RCA clean, stereo; clipping seen earlier was the user's speaker).
  * **USB from our programs:** `ubusb.h` calls U-Boot's USB stack (link addr + reloc_off, $t9 thunk): usb_get_dev_index 0x80122a78 (struct 1352 B), usb_bulk_msg 0x80122cb4, usb_control_msg 0x80122d50. Dev offsets devnum 0, speed 4, maxpacketsize 104, VID 264, PID 266, status 1300, act_len 1304. EHCI transfers synchronous: bulk IN without data blocks ~5 s ("EHCI timed out on TD"). Orico USB 2.0 hub works (sometimes 2nd `usb reset`); `usb port [num]` selects root port (0 = USB-A socket, 1 = internal WiFi module). `ch340.h` (Linux ch341 sequence) VERIFIED loopback 5/5 at 9600-2000000 baud (`ch340test.c`).
  * **Pico 2 W coprocessor (VERIFIED):** box -> hub -> CH340 -> UART (TXD->GP1, RXD->GP0, GND, 3.3 V) -> Pico 2 W running CircuitPython `pico/code.py` (copy to CIRCUITPY; `pico/main.py` = MicroPython variant, untested). Line protocol, every reply ends with ">" line. `picoterm.c` = console terminal (retries reads up to ~30 s). PING/TEMP/LED/HELP ok, WIFI ok (CircuitPython "Unknown failure 1" quirk handled), HTTP http:// ok (example.com, router page).
  * **Teardown:** SoC Montage M88CS8051B-T, flash XMC XM25QH64AHIG (8 MB SOIC-8), WiFi module BL-M8188FU3 = RTL8188FTV on SoC USB root port 1 (pads 1 GND, 2 D+, 3 D-, 4 VCC 3.3 V, 7 GND), 27 MHz TCXO, J1 = UART header. No free SoC pins wired out.
  * **Internal WiFi RTL8188FTV, own driver (GPL-2.0-only, ported from Linux rtl8xxxu; sources for reference in `rtl8188/`, fw `rtl8188fufw.bin` from linux-firmware):** `rtl8188.h` + `rtl8188_tables.h`. Reg access = vendor ctrl 0x05 (0xc0 read / 0x40 write). eFuse (8188F layout): rtl_id 0x8129 @0, TX power @0x10 (cck_base[6], ht40_base[5] @0x16, diff nibbles @0x1b), channel plan @0xb8, xtal_k @0xb9, VID/PID @0xd0, **MAC @0xd7 = b4:6d:c2:24:84:20**. SYS_CFG 0x04441525 (cut B). Endpoints: EP1 IN bulk (RX), EP2/EP3 OUT bulk (TX), 512 B. VERIFIED: `rtlhello.c` (regs + eFuse), `rtlfw.c` (power on + firmware running: MCU_FW_DL 000501c6), `rtlscan.c` (full init 415 ms, scan ch 1-13, 7 APs found), `rtlprobe.c` (TX of probe requests via txdesc40 on EP2, 7 probe responses to our MAC). Silent channels cost one ~5 s EHCI timeout.
  * Files lost on 2026-09-24 by a git reset/pull outside the session were recreated from the conversation (rtlhello.c now uses rtl8188.h; `rtljoin.c` intentionally not recreated).

---

## STATUS AT END OF SESSION 2026-09-24 (start here in a new session)

**Works on hardware (all from U-Boot, no stock firmware needed):**
- 1080i HDMI output + own OSD graphics (`osdsetup.h`, `osd.h`, libgfx NC5874 backend), autoboot from USB stick (`autoboot.scr` in flash).
- Remote control (`ir.h`), STANDBY button, LEDs (`board.h`).
- Audio out HDMI + RCA (`audio.h`), `audplay.c`, `wavplay.c`.
- U-Boot's USB stack from our programs (`ubusb.h`), USB hub, CH340 (`ch340.h`).
- Pico 2 W coprocessor over CH340 (`pico/code.py`, `picoterm.c`): WiFi + plain HTTP.
- Internal RTL8188FTV with own driver: firmware, init, scan (`rtlscan.c`), management-frame TX (`rtlprobe.c`).

**Workflow reminders:**
- `setenv a 0x80008000` after every box reset (not saved).
- USB stick drive letter on the PC can change (G:, H:). "No medium found" = stick not in the PC.
- WiFi chip programs: fatload the program and `rtl8188fufw.bin` (fw last) while the stick is visible, then `usb port 1`, `usb reset`, `go ${a} 82000000 ${filesize}`.
- `${filesize}` = size of the LAST fatload.
- Before building audplay: `python mksine.py > sine256.h`.
- **Commit work to git regularly** (license decision pending: repo LICENSE is GPL-3.0, but rtl8188* files are GPL-2.0-only derived from Linux -> switch repo to GPL-2.0-or-later, or keep rtl8188* out of the repo).

**Possible next steps (user to pick):**
1. MP3 decoder on the box (fixed-point, e.g. Helix) -> play .mp3 from USB with `audio.h`.
2. Internet radio via the Pico: faster binary protocol + higher baud on the CH340 link, Pico buffers the stream, box decodes MP3 (needs 1).
3. Non-blocking USB for our programs (own minimal EHCI driver or patch U-Boot's EHCI timeout).
4. Apps: game (remote + libgfx + audio), music player UI, weather/clock screen via Pico HTTP.
5. Remote key names for `ir.h`.
6. Second CPU core (AV core) for own code.

---

## 5. Project Files

### `link.ld` (Custom Linker Script)
```ld
OUTPUT_FORMAT(elf32-tradlittlemips)
OUTPUT_ARCH(mips)
ENTRY(_start)

SECTIONS
{
    . = 0x00000000;
    .text : { *(.text) }
    .data : { *(.data) }
    .bss : { *(.bss) }
}